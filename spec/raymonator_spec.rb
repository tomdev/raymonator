require 'spec_helper'

describe Raymonator do
  it 'has a version number' do
    expect(Raymonator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
